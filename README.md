# ChatBox
[C++ / Windows ONLY] A lightweight LAN messaging tool with UDP broadcast for instant text chat and TCP streams for multi‑receiver file transfers. It features AES‑32 user credential encryption, auto‑detected subnet broadcast, persistent chat history, rich‑text display, file preview, and a single‑instance lock – all in a compact Windows native GUI.
